#pragma once

#include <grammar.h>

#ifdef _DEBUG

/**
 * @ref grammar
 * @ref symbols
 */
void symbols_logi(Grammar grammar, Symbol symbols);

/**
 * @ref grammar
 */
void grammar_log(Grammar grammar);

#endif
